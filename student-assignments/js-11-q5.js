// What is the result of the code below?
const hello = "what's up aps?!";
const obj = {
  func() {
    return hello;
  },
};
console.log(obj.func()); // answer is?:
// what's up aps?!
// ----------------------------
// we call func of abj 
// what`s func ? hello
// what`s hello ? "what's up aps?!"
